/** pfc_targetsに未設定のユーザー向けの既定値。クライアント（食事ログ画面）とサーバー（LINE返信の
 * 目標比表示）の両方から使うため、"server-only"は付けない素のデータファイルにしている。 */
export const DEFAULT_PFC_TARGET = { calories: 2380, protein_g: 175, fat_g: 65, carb_g: 275 };
